import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const User = () => {
    return (
        <View>
            <Text>User</Text>
        </View>
    );
}

const styles = StyleSheet.create({})

export default User;
